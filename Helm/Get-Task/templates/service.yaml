apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-get-task
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: {{.Release.Name}}-get-task
  ports:
  - port: 80
    targetPort: 80
